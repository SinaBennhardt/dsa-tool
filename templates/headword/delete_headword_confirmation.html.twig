{% extends "campaign_layout.html.twig" %}

{% block content %}

        <h3>Möchtest du das Schlagwort <em>"{{ headword.headwordName }}"</em> wirklich löschen?</h3>

        <p>Das Schlagwort wird dabei von allen Beiträgen entfernt. Diese Aktion kann nicht rückgängig gemacht
            werden.</p>

        {{ form_start(deleteConfirmationForm) }}

        <a class="btn btn-light" role="button" href="{{ path('delete_headword') }}">
            Zurück
        </a>

        {{  form_label(deleteConfirmationForm.submit) }}
        {{ form_widget(deleteConfirmationForm.submit, {attr: {class:"btn btn-danger"}}) }}

        {{ form_end(deleteConfirmationForm) }}


{% endblock %}