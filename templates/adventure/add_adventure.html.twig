{% extends "campaign_layout.html.twig" %}

{% block content %}

    <h1>Neues Abenteuer</h1>

    {{ form_start(addAdventureForm) }}
    <div class="form-group">
        <h2>{{ form_label(addAdventureForm.title) }}</h2>
        {{ form_widget(addAdventureForm.title, {attr:{size:"90%"}}) }}
        {{ form_errors(addAdventureForm.title) }}
    </div>

    <div class="form-group">
        <h2>{{ form_label(addAdventureForm.blurb) }}</h2>
        {{ form_widget(addAdventureForm.blurb, {attr:{rows:"10", cols:"90%",
            placeholder: "Schreibe hier deine Zusammenfassung hin"}}) }}
        {{ form_errors(addAdventureForm.blurb) }}
    </div>

    <a class="btn btn-secondary" role="button"
       href="{{ path('change_campaign', {campaignId: campaignId}) }}">Änderungen VERWERFEN</a>
    {{ form_label(addAdventureForm.submit) }}
    {{ form_widget(addAdventureForm.submit, {attr:{class:"btn btn-success"}}) }}

    {{ form_end(addAdventureForm) }}


{% endblock content %}