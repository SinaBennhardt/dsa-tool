{% extends "campaign_layout.html.twig" %}

{% block content %}

    <h3>Möchtest du die Kampagne <em>"{{ campaign.title }}"</em> wirklich löschen?</h3>

    <p>Dabei werden auch alle in der Kampagne enthaltenden ABENTEUER gelöscht! Diese Aktion kann nicht rückgängig gemacht
        werden.</p>

    {{ form_start(deleteConfirmationForm) }}

    <a class="btn btn-light" role="button" href="{{ path('change_campaign', {campaignId: campaignId}) }}">
        Zurück
    </a>

    {{  form_label(deleteConfirmationForm.submit) }}
    {{ form_widget(deleteConfirmationForm.submit, {attr: {class:"btn btn-danger"}}) }}

    {{ form_end(deleteConfirmationForm) }}


{% endblock content %}